<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit <%= clients[0].first_name %> <%= clients[0].last_name %></title>
    <style>
        * {
          margin: 0;
          padding: 0;
          box-sizing: border-box;
        }
    
        body {
          display: flex;
          justify-content: center;
          align-items: center;
          height: 100vh;
          font-family: Arial, sans-serif;
          background-color: lightblue;
        }
    
        .add-form {
          width: 300px;
          padding: 20px;
          background-color: #ffffff;
          box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
          border-radius: 8px;
          text-align: center;
        }
    
        .add-form h2 {
          margin-bottom: 20px;
          color: #333;
        }
    
        .add-form label {
          display: block;
          text-align: left;
          margin-bottom: 5px;
          color: #666;
        }
    
        .add-form input {
          width: 100%;
          padding: 10px;
          margin-bottom: 15px;
          border: 1px solid #ccc;
          border-radius: 4px;
        }
    
        .add-form button {
          width: 100%;
          padding: 10px;
          background-color: black;
          color: white;
          font-size: 16px;
          border: none;
          border-radius: 4px;
          cursor: pointer;
        }
    
        .add-form button:hover {
          background-color: black;
        }
    
        .error-message {
          color: red;
          margin-top: 10px;
        }
    </style>
</head>
<body>
    <!-- Form to edit the data of a client. Displays name of client-->
    <div class="add-form">
        <h1>Edit <%= clients[0].first_name %> <%= clients[0].last_name %></h1>
        <br><br>
        <form action="/changeClientInfo" method="post">
            <!-- hidden type to keep client_id avaliable for post method to edit-->
            <input type="hidden" id="client_id" name="client_id" value="<%= clients[0].client_id %>">
            <label for="firstName">First Name</label>
            <input type="text" id="firstName" name="first_name" value="<%= clients[0].first_name %>">
      
            <label for="lastName">Last Name</label>
            <input type="text" id="last_name" name="last_name" value="<%= clients[0].last_name %>">
          
            <label for="phone">Phone Number</label>
            <input type="text" id="phone_number" name="phone_number" value="<%= clients[0].phone_number %>">
         
            <button type="submit">Save Changes</button>
            <p id="error-message" class="error-message"></p>
        </form>
        <!-- Form to go back to client list page-->
        <form action="/cancel" method="get">
            <button type="submit" class="cancel-button">Cancel</button>
        </form>
    </div>
    
</body>
</html>